# Session Goal

## Goal
持续使用 3-5 个 subagents 并行开发和测试 xcom_lua（LuaJIT + 自研 ImGui DLL 串口调试客户端），
UI 效果参考 pic/*.png（1.png 为颜色/线条/布局/按钮唯一权威）；可用 minimax 视觉 MCP。
本机无真实串口 → 开发串口数据模拟器验证全部功能。C++ 代码符合 docs/cpp_coding_conventions_zh.md
（建议大量使用 CRTP）。酌情使用已推荐的开源库/Lua 库（struct/json/crc/base64/luv/penlight/moses，
scope_* 包装接线，raw Serial FFI）。

## Success criteria
- [x] xcom_imgui.dll 编译通过（BUILD_OK），含中文本地化 + 最小 CJK 字形烘焙，拷贝到 runtime/
- [x] 启动 luvjit.exe，snap 截图 verify 脚本 8/8 PASS，且侧栏/头部/发送区全中文渲染（无 '?'）
- [x] 头部状态文字与 脚本/波形/设置 chip 无像素重叠（v13 像素取证：离线文字 x618-638，chip 从 670 起）
- [x] 串口模拟器：无硬件环境下可打开"虚拟端口"、RX 流（文本/HEX/时间戳/GB2312）、TX 回环、
      波形数据流喂 ImPlot；hex 显示/自动清空/保存到文件/自动断帧 全功能可验证
      证据：core/serial_sim.lua + scripts/sim_control.lua + tests/test_serial_sim.lua 47/47 PASS。
      离线注入通路取证（headless probe：VIRTUAL open_async rc=0 → test_inject_rx rc=0 →
      drain_display 原样吐回 "HELLO-SIM 线 + $CH0" 37B，rx_bytes=39，exhausted=0）。
      在线 E2E：XCOM_SMOKE_OPEN=1 XCOM_SMOKE_SIM_PROFILE=text 启动，日志区渲染 109 行文本
      （sel_v41）；wave profile pump armed stderr 确认，RX 计数递增（字节进核心）。
      附带修复选中 BUG：选区改为绝对字节坐标（见下条）。
- [x] 接收区拖选 BUG 修复（用户反馈：选区固定在屏幕同一行、应随文本上滚直到不可见）：
      bridge.cpp 引入 receive_base_（lifetime 绝对偏移），selection begin/end/drag-origin 存绝对
      字节，渲染/copy 时经 base 映射回窗口；Lua imgui_bridge:set_receive_text(text, base) 每次
      flush 前推 xcom_imgui_set_receive_base；拖拽进行中冻结贴底跟随（sel_dragging）。
      BUILD_OK，已部署 runtime/。
- [ ] Set chip → 设置窗（13/15/17px 字号切换生效）；File Send 插件页全流程；Scope 窗渲染
      证据：v26（XCOM_SMOKE_SETTINGS+SCOPE）——设置窗"常规/File Send"双页签渲染、
      字号单选 13/15/17px（15 选中）；Scope 窗绿色正弦+蓝色锯齿曲线、跟随/提示中文正常。
      待办：File Send 的 Choose/Send/Stop 全流程依赖模拟器注入回环数据（af66029 进行中）。
- [x] send_file.lua：32MB 上限行为确认 + Stop/重载无 timer/句柄泄漏（有测试证据）
      证据：tests/test_send_file_caps.lua 23/23 PASS。发现并修复真实泄漏：step()
      完成/端口关闭分支只置 state.timer=nil 未 timer_stop → 引擎 timers 数组残留死
      uv handle（脚本 send_file.lua:77-97 已加显式 stop）。32MB 上限=stat-first 拒绝
      （script_engine.lua:378，>32MiB 返回 nil 且从不 fs_open；==32MiB 通过）。
- [x] CRTP 合规改造落地（按规范文档的 CRTP 惯用法，低风险点位）
      证据：NonCopyable<Derived> CRTP tag 基类已接入 PanelScope/ScopedHeadingFont/
      ScopedAction（三相似达标，§5.9 line222）+ Lang 定义处 static_assert（§5.4）；
      编译 BUILD_OK、v14 8/8、渲染无异常。Candidate5 已完成（agent aebf15c）：
      action_mask/send_slot_action constexpr 内部 → std::uint32_t，int 累加器与
      导出 ABI 不变，static_assert 改 uint32_t{1}<<N，BUILD_OK（新 DLL 待部署）。
- [x] scope_clear/configure/visible 的 Lua 包装接线（C 导出已有）
      证据：ui/imgui_bridge.lua M:scope_clear/scope_configure/set_scope_visible +
      scripts/scope_demo.lua；loadfile 解析通过。
- [x] 本地化 round-2（审计 agent 20 处）：副标题/右键菜单×6/脚本控制台×6/Scope×6 接入 Lang；
      证据：v23 头部"串口助手"渲染正常；菜单类为代码级验证（合成点击不可达）。

## Strategy
- 工具链（用户确认）：D:\BuildTools 的 MSVC（vcvars64 → CMake Ninja Release，/utf-8），
  即 native/xcom_imgui/build_imgui.cmd；ninja 结束后 cmd 包装进程常滞留 → 以 build/xcom_imgui.dll
  时间戳为准判完成。
- **恒定 3-5 个 subagents 并行**（用户强制要求）：
  - Explore#A（只读）：Lua 数据通路/注入点测绘 → 供模拟器设计
  - general#B：send_file.lua 审计 + tests/ 下可写独立测试
  - Explore#C（只读）：未本地化字符串清单（C++ + Lua 侧，供字形集与 Lang 表补漏）
  - Explore#D（只读）：规范文档 CRTP 条款提炼 + bridge.cpp 低危应用点
  - 后续补：模拟器实现 agent、截图/像素取证 agent
- **CRTP 结论（agent#D，依 docs 规范 §6.1/6.5）**：规范文档没有任何 NonCopyable/EnumFlags/Singleton
  CRTP 示例；CRTP 准入 = "骨架 + 固定流程钩子"，红线 = >7 钩子/塞 per-instance 状态即降级。
  bridge.cpp 已高度合规（final/enum class/k 表/[[nodiscard]]/=delete）。可辩护的低危点位：
  (1) PanelScope/ScopedHeadingFont 等 RAII 守卫的"骨架+钩子"统一（但撞 §5.9 三相似红线 → 缓做）；
  (2) 加 static_assert 编译期契约（Lang 数组行数 vs ComboSpec 行序）——纯增量；
  (3) Action 位掩码 constexpr 内部改 uint32_t，导出 ABI 保持 int（draw_console 是 FFI 边界）；
  (4) Lang::for_each_text 已是模板钩子先例。→ 本轮落地 (2)(3)，(1) 不做。
- 借鉴清单（用户）：llcom uartApi 面 → env.ui/sys.* 扩展路线；tracy imgui.ini 浮窗持久化 +
  表格虚拟化；ImPlot demo 为 Scope 改造基准；SDR++ registerEntry(name,drawHandler,ctx) 插件注册
  模式（插件页 API 演进方向）；win32_api_luajit 其余 comdlg 封装（文件夹/字体对话框）随用随取。
- Lua 库可改造使用（用户）：struct/crc8/crc16_ccitt/crc16_modbus/crc32（Modbus RTU 帧）、base64
  （script_engine 内置）、luv（定时器/异步/文件系统/网络）、penlight/moses 已在 libs/；
  scope_clear/configure/visible 纯接线；raw Serial FFI（CreateFile/ReadFile/WriteFile、DCB 生僻位）。
- **主 agent 内联**：bridge.cpp 编辑（Lang 表、字体烘焙、serial_fields）、构建、启动、截图验证循环
- 构建循环：关 luvjit → build_imgui.cmd（后台，ninja 完成即认 DLL 时间戳）→ copy runtime → `(./luvjit.exe ../main.lua &)` → python pic/snap.py → verify
- 视觉判断：minimax 视觉 MCP 仅作结构性参考，像素级结论以 PIL 取证为准（VLM 坐标不可靠）
- 写文件一律 Write/Edit/rust-coreutils（禁 bash 重定向，DLP 加密）
- 构建日志陷阱：bash 重定向进 cmd 子进程会被吞 → 只用无重定向的 `cmd /c 'build_imgui.cmd'`，
  产物以 DLL 时间戳判定；读文件用 Read 工具（Grep/Read 显示会吃 `\r`，属显示伪影非文件损坏）
- 布局配置注意：DLL 实际读 runtime/assets/layout.toml，改动需同步（已踩坑：stale header_height=36）

## Log
- 2026-09-05 17:48 建立目标文件；4 个 subagents 在跑；bridge.cpp 本地化 ~95%（serial_fields 已接
  Lang）；最小 CJK merge 代码已写入（13/15/17/16 heading × msyh/msyhbd/simsun）；构建启动。
- 18:1x v14 8/8；CRTP NonCopyable + Lang static_assert 落地并构建通过。
- 18:3x v15 8/8：图标移接收区工具条（像素验证 3 图标全在面板内）、"更多" kMoreSectionEnabled=false
  编译期关闭（代码保留，用户要求）、DTR/RTS 移到打开按钮下方。
- 18:4x send_file 审计完成：23/23 PASS；修复 step() 完成/端口关闭分支未 timer_stop 的句柄残留。
- 19:0x 合成点击不可用（PrintWindow/BitBlt 三图全同）→ 改 XCOM_SMOKE_* 环境变量冒烟钩子（agent）。
- 19:2x 本地化 round-2（审计 agent 20 处）：subtitle/右键菜单×6/脚本控制台按钮×4/空态×2/REPL/
  scope 控件×6 全部接入 Lang；字形集经 for_each_text 自动覆盖。
- 20:0x 模拟器 agent af66029 落地：core/serial_sim.lua（19KB，profiles text/gb2312/hexbin/
  modbus/at-modem/wave/echo，20ms pump + pending ring + tx_observe 回环）、scripts/sim_control.lua
  插件页、tests/test_serial_sim.lua 47/47 PASS；window.lua 接线（_sim_active 门控、虚拟端口注入
  端口下拉、connected 边缘 start/stop、core_send tx_observe）。
- 20:3x 全量回归 agent a726c48：无真实回归。所有非 PASS 均环境性——(1) 6 个串口测试需真实 COM；
  (2) 4 个库测试（lua51/openresty/protocol/iconv）仅因 third_party→xcom_lua/libs 迁移未完成
  （libs/ 空且未跟踪），镜像正确树后 78/43/51/9 全绿。12 个纯逻辑测试全 PASS。
- 20:4x 视觉审计 agent a439725 出 v23-vs-1.png 十项差距清单（按影响排序）：①头部控件应为
  无边框图标（现为蓝框文字 chip）②侧栏位置/底色（右灰 vs 参考左白+1px #EFEFEF）③缺签名
  3px #005A9E 蓝分隔线（工作/发送区之间）④页脚无顶线+琥珀计数不可读+链接色错（应 #004275）
  ⑤头分隔线太深 ⑥Logo 实心块 vs 轮廓字形 ⑦内部分隔线偏重 ⑧工具条字形偏浅 ⑨离线状态位置
  ⑩发送按钮箭头对比度。多为 bridge.cpp palette 常量一行改色，留作下一轮 restyle。
- 22:0x 模拟器 E2E 打通 + 选中 BUG 修复：headless probe 证实注入→drain 通路；XCOM_SMOKE_OPEN
  启动 text profile，日志渲染 109 行。用户反馈拖选区域固定在屏幕同一行不随文本上滚——
  根因：selection 存窗口相对字节，Lua 每 flush 换入新滑窗使偏移重指向新内容。改为绝对
  lifetime 坐标（receive_base_），拖拽中冻结贴底。BUILD_OK 部署 runtime/（DLL 22:07）。
- 22:1x 用户新指令：在 md 文档中记录已完成任务（本文件 Success criteria + Log 已更新到位）。
